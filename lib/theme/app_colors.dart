import 'package:flutter/material.dart';

/// Brand color tokens for the CRM Agent application.
class AppColors {
  AppColors._();

  /// The primary brand color (vibrant red).
  static const Color primaryColor = Color(0xFFE53935);

  /// A soft pink/red tint used for light containers or border highlights.
  static const Color primaryColorLight = Color(0xFFFEE2E2);

  /// The default scaffold background color (off-white/light grey).
  static const Color backgroundColor = Color(0xFFF7F8FA);

  /// Dark slate color for prominent text and headings.
  static const Color textDark = Color(0xFF1E293B);

  /// Muted grey color for captions, subtitles, and input hints.
  static const Color textMuted = Color(0xFF64748B);

  /// Light grey color for default borders and subtle separators.
  static const Color borderLight = Color(0xFFE2E8F0);

  /// Alert red color used for form validation errors and indicators.
  static const Color errorColor = Color(0xFFEF4444);

  /// Light red background color for error notification banners.
  static const Color errorBackground = Color(0xFFFEF2F2);
}
